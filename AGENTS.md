# 宠物健康助手项目注意事项

本项目由以下三个相互独立的子项目组成：

## 目录说明

- `app/`：小程序端，面向宠物主人，提供宠物健康助手相关功能。
- `server/`：服务端，后续用于开发接口、业务逻辑、数据存储及鉴权等功能。
- `admin/`：管理后台，后续用于开发运营管理、内容管理和数据管理等功能。

## 开发注意事项

1. 三个子项目的代码、依赖和配置应分别放在各自目录中，不要混放。
2. 启动命令、环境要求和开发说明应维护在各子项目自己的 README 文件中。
3. 密钥、数据库密码、接口令牌等敏感信息不得提交到版本库；应使用环境变量或本地配置文件。
4. 小程序端和管理后台统一通过服务端接口访问业务数据，不应直接连接数据库。
5. 服务端接口发生变更时，应同步更新接口文档，并确认小程序端和管理后台是否需要适配。
6. 各子项目生成的依赖目录、构建产物、日志和本地环境配置应加入忽略规则。

## 界面字号规范

后续新增或调整界面文字样式时，应优先使用下列标准字号，不要随意引入规范外的字号；如确有特殊场景，应先说明设计原因并保持同类界面一致。

| 字号 | 建议用途 |
| --- | --- |
| `18rpx` | 徽标、极小辅助信息 |
| `20rpx` | 日期、提示、状态说明 |
| `22rpx` | 底部导航、标签、次要说明 |
| `24rpx` | 辅助正文、宠物属性 |
| `28rpx` | 默认正文、输入框、主要按钮 |
| `32rpx` | 卡片标题、消息标题 |
| `36rpx` | 顶部导航标题、页面标题 |
| `40rpx` | 页面重点标题 |
| `48rpx` | 统计数值、个人名称 |
| `52rpx` | 首页宠物名称等视觉主标题 |
| `58rpx` | 综合评分等特殊大数字 |
