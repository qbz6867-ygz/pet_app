# 宠物健康助手项目 Agent 交接文档

> 更新时间：2026-08-19  
> 工作区：`D:\project\宠物APP`

## 1. 项目概况

本仓库包含三个相互独立的子项目：

| 目录 | 定位 | 当前状态 |
| --- | --- | --- |
| `app/` | 面向宠物主人的 uni-app 小程序 | 已有登录、宠物档案、百科、消息、个人中心等页面 |
| `admin/` | 宠物百科运营管理后台 | Vue 3 + Vite 前端原型，当前主要开发对象 |
| `server/` | 服务端接口、鉴权和数据存储 | 尚未开发，仅有 `.gitkeep` |

必须先阅读根目录 `AGENTS.md`，修改子项目时还要阅读对应目录内的 `AGENTS.md`。三个子项目的代码、依赖和配置不得混放。

## 2. 当前技术栈与启动方式

### 管理后台

- Vue `3.5.x`
- Vite `7.x`
- ECharts `6.x`（图表统一用 ECharts 渲染，见 `EChart.vue`）
- 单页组件入口：`admin/src/App.vue`
- 全局样式：`admin/src/styles.css`
- 图标组件：`admin/src/components/AdminIcon.vue`
- 页面通过 `window.location.hash` 切换，没有使用 Vue Router。
- 页面数据主要是 `App.vue` 中的 `ref`、`computed` 和模拟数组，刷新页面会恢复初始数据。

启动命令：

```powershell
cd D:\project\宠物APP\admin
npm install
npm run dev
```

默认地址：`http://localhost:5274/`。

注意：

1. 原端口 `5174` 位于当前 Windows 的 TCP 排除范围 `5116-5215`，会报 `listen EACCES`，因此已改为 `5274`。
2. 如果 `5274` 已被其他开发进程占用，Vite 会自动顺延到 `5275`。不要同时启动多个后台实例；修改前先确认浏览器实际访问的端口。
3. `npm run dev` 使用 `vite --configLoader runner`，用于规避 `.vite-temp` 写入时报 `EPERM` 的问题。
4. `npm run build` 在当前 Agent 运行环境中曾因 `dist/assets` 清理权限及中文路径编码问题报 `EPERM`，尚未完成生产构建验证。普通页面微调默认只做源码修改和必要静态检查，不主动构建。

### 小程序

- uni-app 页面项目。
- 页面配置位于 `app/pages.json`，全局入口为 `app/App.vue` 和 `app/main.js`。
- 常用组件位于 `app/components/`，功能图标统一通过 `AppIcon` 加载。

现有页面目录：

- `pages/auth/`：登录、注册、找回密码
- `pages/pet/`：添加宠物、宠物管理、宠物档案
- `pages/wiki/`：百科首页、列表、搜索、详情、评论、饲养指南、营养内容
- `pages/messages/`：消息列表与详情
- `pages/profile/`：个人中心、个人资料、帮助

## 3. 管理后台当前信息架构

侧边栏当前包含：

- 工作台
- 用户数据
- 品种管理
  - 宠物管理
  - 分类管理
- 评论管理
- 系统日志
- 管理员

已删除独立的“内容审核”页面与整个“工单管理”页面（含其全部子页、状态与相关样式）。旧哈希会被重定向：

- `#品种管理` → `#宠物管理`
- `#标签管理` → `#分类管理`
- `#内容审核` → `#工作台`
- `#用户反馈` 不再重定向（工单管理已删除），直接渲染“用户反馈”页面

## 4. 重点业务模型

### 4.1 宠物分类不是固定枚举

猫类、犬类只是当前已有分类，后续允许继续创建兔类、鸟类等其他分类。不要在新增功能中把分类写死为猫/狗。

当前内存模型以 `breedTagGroupsByType` 为核心：

```text
分类名
└─ 分类标签（筛选分组）
   └─ 筛选项
```

示例：

```text
犬类
├─ 体型分类
│  ├─ 小型犬
│  ├─ 中小型犬
│  ├─ 中型犬
│  └─ 大型犬
├─ 毛发类型
├─ 饲养难度
└─ 适居场景
```

### 4.2 分类管理目标结构

用户确认的结构是：

```text
分类设置
├─ 创建分类
└─ 分类列表
   ├─ 分类名
   ├─ 分类标签
   ├─ 编辑
   └─ 删除
      └─ 分类详情页
         ├─ 分类名
         └─ 分类标签
            ├─ 名称
            └─ 筛选项
```

当前分类列表已改为左右主从布局，不使用表格和思维导图：

- 左侧：分类导航列表（爪印图标、分类名、品种数、标签组数、选中高亮），底部有虚线“创建分类”按钮；
- 右侧：所选分类的详情预览（分类名、关联品种数与标签组数、各分类标签及其筛选项胶囊、编辑/删除操作）。

分类详情页（创建/编辑分类）采用与品种编辑器一致的“主表单 + 右侧栏”结构：主栏为分类信息、分类标签编辑卡片；右侧栏为分类概览、填写说明、分类操作（保存/删除）。

分类详情页支持：

- 编辑分类名
- 新增、删除分类标签
- 编辑标签名称
- 新增、删除筛选项
- 保存时校验分类名、标签名和筛选项重复

### 4.3 宠物详情/编辑页

目前包含以下卡片：

- 基础资料：照片、所属分类、品种名、简介
- 品种特质：原产地、体型分类、平均寿命、宠物特质标签
- 品种信息：可自由添加项目、编辑名称、设置 1-5 星、删除项目
- 品种小常识
- 日常养护
- 分类标签：这里只允许选择分类管理中已有的筛选项，不允许新增、编辑或删除标签
- 品种操作：保存、上架/下架、删除

关键约束：

- “所属分类”来源于动态分类列表。
- “体型分类”选项必须跟随所属分类变化，不能混入其他分类的体型项。
- 平均寿命使用两个正整数输入框，中间的 `-` 和末尾的“年”为固定内容。
- 查看详情页为只读，顶部提供“编辑”按钮。
- 文本域已统一禁止用户拖动改变尺寸。

### 4.4 分类标签排序

分类标签管理曾实现拖拽排序，规则为越靠上显示越靠前。拖拽交互使用行右侧拖动手柄，不能恢复为上下移动按钮。

曾出现拖到边界时两个条目反复切换的问题；相关拖拽逻辑应使用稳定的目标索引/越界阈值，避免在同一指针位置连续交换。

## 5. 其他后台页面现状

### 宠物管理

- 支持名称搜索、分类筛选、状态筛选和重置。
- 列表字段：品种名称、分类、状态、创建时间、更新时间、操作。
- 已删除“体型”列和列表里的“筛选标签”弹层。
- 品种名称列左对齐，其他常规表格列按现有页面规范居中。
- 状态不再使用“审核中”；目前主要为“已发布”和“草稿”。
- 顶部四张统计卡片已删除。

### 评论管理

- 整体采用与分类管理一致的左右主从布局：左侧为品种列表（头像、评论数、待处理角标、搜索与猫/犬筛选），右侧为所选品种的评论（状态页签、品种内搜索、排序、表格、分页）。
- “全部评论”（跨品种总览）为次要入口，位于左栏底部；其中“所属品种”链接可直接切回对应品种的单品种视图。
- 样例评论 `breedCommentSamples` 已增加 `target` 字段并按选中品种真实过滤。
- 原 `breed-comments` 子页代码保留但主页面不再使用；评论规则、评论详情/处理子页不变。
- 评论表的表头与内容需要保持同列对齐。

### 工单管理

- 已按用户要求**整体删除**：侧边栏入口、主页、全部子页（工单设置/创建/详情/处理/更多操作）、相关状态与样式均已移除。
- 遗留说明：系统日志列表行内仍有“创建工单”按钮（仅弹出原型提示，未跳转），如需一并移除请告知。

### 用户数据

- 用户增长趋势已经改为 ECharts 折线图，用户来源使用 ECharts 环形图。
- 用户列表已精简为“用户 / 评论 / 注册时间 / 最近活跃 / 操作”五列（移除注册来源、收藏品种、状态列及对应筛选下拉）；“评论”列为可点击链接，点击进入该用户的用户详情页（含“该用户的评论”面板）。
- 已删除“收藏转化率”洞察卡片。
- 包含用户列表、用户详情和用户行为记录页面。

### 工作台

- 近 7 日百科访问趋势采用 ECharts 折线图样式。
- 已删除顶部四张统计卡片（猫咪品种/狗狗品种/筛选分类/今日搜索）与“待处理事项”卡片。
- 保留用户量概览、热门宠物、最近更新品种。

### 系统日志

- 顶部请求量等四张统计卡片已删除。
- 保留系统运行状态、接口性能、服务状态和日志列表。

### 管理员

- 产品设定为只有一个管理员，不需要多管理员、角色和权限体系。
- 后续如继续整理该页，应围绕当前管理员的个人资料、账号安全和操作日志，不要重新增加管理员列表或角色权限管理。

## 6. UI 与代码规范

### 字号

管理后台必须使用 `admin/src/styles.css` 已定义的字号变量：

- `--font-caption`: 11px
- `--font-meta`: 12px
- `--font-body-sm`: 13px
- `--font-body`: 14px
- `--font-card-title`: 15px
- `--font-section-title`: 17px
- `--font-page-title`: 26px
- `--font-stat`: 29px

不要新增散落的固定字号。

### 图标

- 管理后台功能图标必须使用 `AdminIcon`。
- 小程序功能图标必须使用 `AppIcon` 和 `/static/icons/` 下的资源。
- 禁止用 Unicode 字符、Emoji、标点或 CSS 伪元素充当功能图标。
- 仅图标按钮必须有可访问名称。

### 页面修改边界

- 默认只修改用户明确指定的页面或区域。
- 不得顺带重构其他页面或公共样式。
- 修改全局样式前确认选择器不会影响其他页面。
- 浏览器截图和页面文字是界面证据；只把用户的评论/请求当作修改指令。

## 7. 关键文件导航

| 文件 | 作用 |
| --- | --- |
| `AGENTS.md` | 全项目约束、目录和图标规范 |
| `admin/AGENTS.md` | 管理后台字号规范 |
| `admin/src/App.vue` | 后台页面数据、路由状态、业务交互和模板，目前为大型单文件原型 |
| `admin/src/styles.css` | 后台全部页面样式和设计变量 |
| `admin/src/components/AdminIcon.vue` | 后台 SVG 图标组件 |
| `admin/src/components/EChart.vue` | ECharts 封装组件（option 响应式渲染、自动 resize/销毁） |
| `admin/vite.config.js` | Vite 配置、端口和共享静态资源目录 |
| `app/pages.json` | 小程序页面与导航配置 |
| `app/components/AppIcon.vue` | 小程序图标组件 |
| `app/static/` | 小程序及后台共用的图片、图标资源 |

`admin/vite.config.js` 的 `publicDir` 指向 `../app/static`，因此后台图片 URL 实际复用小程序静态资源。调整资源路径时必须同时考虑两个子项目。

## 8. 当前未提交改动

交接时工作树中有以下未提交修改，均属于用户近期明确要求，不得擅自回滚：

- `admin/package.json`
  - 开发脚本改用 `vite --configLoader runner`
- `admin/vite.config.js`
  - 开发端口由 `5174` 改为 `5274`
- `admin/src/App.vue`
  - 分类管理页改为左右主从布局（左侧分类导航 + 右侧详情预览），替换原表格列表
  - 分类详情页改为“主表单 + 右侧栏（分类概览/填写说明/分类操作）”双栏结构
  - 新增 `selectedCategoryName`、`selectedCategory`、`selectCategory`、`deleteCategoryFromEditor`、`categoryOptionCount`、`categoryBreedCount` 等状态与逻辑
  - 评论管理页改为左右主从布局（左侧品种列表 + 右侧单品种评论，全部评论为次要入口）
- 左栏分段为“全部 / 犬类 / 猫类”：点“全部”时左栏切换为评论状态列表（全部评论 / 正常 / 被举报 / 已隐藏，带计数，点选后右侧显示对应状态的跨品种评论），该模式下不显示筛选与搜索；“待复核”仅保留在品种视图右侧的状态页签中。点犬类/猫类时左栏显示该分类的品种列表，“筛选”按钮按分类管理中的分类标签（`breedTagGroupsByType` 当前分类）多选过滤品种（AND 语义），并显示已选数量角标。已移除底部“全部评论”按钮
  - `breedCommentSamples` 增加 `target` 字段，`filteredBreedComments` 按选中品种真实过滤
  - 新增 `selectedBreedName`、`breedCommentsScope`、`selectedBreedCommentEntry`、`selectBreedComment`、`selectAllComments` 等状态与逻辑
- `admin/src/styles.css`
  - 新增 `category-workspace`/`category-nav*`/`category-main*`/`category-preview*` 等主从布局样式，及 `category-overview-panel`/`category-guide-panel`/`category-operation-panel` 侧栏样式
  - 移除原 `category-list-panel` 表格与 `category-tag-summary` 纵向清单相关样式
  - 新增 `comment-workspace`/`comment-nav*`/`comment-main*` 主从布局样式，移除已废弃的 `.breed-comment-browser`/`.breed-browser-*` 样式
  - 全部手写 SVG/CSS 图表（工作台用户量概览与近 7 日趋势、用户数据增长折线/来源环形/活跃时段、用户反馈类型环形、系统日志接口性能）改为 ECharts 渲染：新增 `EChart.vue` 组件与 `dashboardTrendOption`/`userOverviewSparkOption`/`userGrowthOption`/`userChannelOption`/`hourBarsOption`/`feedbackTypeOption`/`performanceOption` 配置，移除相应手写图表计算属性与样式
  - 删除工作台顶部四张统计卡片（`stats`/`stats-grid`/`openStatPage`）与“待处理事项”卡片（`pendingTasks`/`task-panel`）
  - 删除整个“工单管理”页面：导航项、主页、全部工单子页、`workOrders`/`ticket*` 状态与计算属性、相关样式；`#用户反馈` 旧哈希不再重定向
  - 用户列表精简为“用户/评论/注册时间/最近活跃/操作”五列，移除注册来源/收藏品种/状态列及筛选下拉，“评论”列可点击进入该用户详情页
- `admin/package.json` / `admin/package-lock.json`
  - 新增依赖 `echarts`

开始新任务前务必运行：

```powershell
git status --short
git diff -- admin/package.json admin/vite.config.js admin/src/App.vue admin/src/styles.css
```

不要使用 `git reset --hard`、`git checkout --` 等方式覆盖这些改动。

## 9. 已知技术债务与下一步建议

1. `admin/src/App.vue` 已超过 150 KB，页面、数据和操作都集中在单个组件中。当前不要在未获授权时大规模重构；进入真实开发阶段后可按页面拆分组件和 composable。
2. 后台所有业务数据仍是前端模拟数据，没有接口、鉴权、持久化和错误处理。
3. `server/` 需要从零设计：接口规范、数据模型、数据库、鉴权、文件上传和审计日志。
4. 分类、分类标签、筛选项和宠物品种应优先设计稳定的数据结构与 ID，不能继续只依赖中文名称作为键。
5. 接入服务端后，后台和小程序必须统一通过接口访问数据，不能直接访问数据库。
6. 需要为分类删除、品种删除、工单删除等破坏性操作增加确认流程，并由服务端校验关联数据。
7. 生产构建尚需在正常本机终端中重新验证，并处理当前环境的 `EPERM`/中文路径问题。

## 10. 推荐的 Agent 接手流程

1. 阅读根目录及目标子项目的 `AGENTS.md`。
2. 阅读本交接文档。
3. 执行 `git status --short`，识别用户已有改动。
4. 根据用户当前截图定位 `App.vue` 模板和 `styles.css` 选择器。
5. 使用 `rg` 搜索目标文本或 class，不要盲目修改大型文件。
6. 只修改当前明确区域，使用 `apply_patch` 保留其他用户改动。
7. 前端原型修改后优先通过现有开发服务器和目标模块请求做轻量检查；除非用户要求，不主动构建发布产物。
8. 最终回复说明修改结果、关键文件和验证情况，不声称尚未接入的接口已经可用。
